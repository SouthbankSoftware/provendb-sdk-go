package anchor

import (
	"context"
)

func TestProofHandle_Update(ctx context.Context) {
	// // Create a new connection
	// conn, err := grpc.Dial("", grpc.WithInsecure(), grpc.WithPerRPCCredentials(NewServiceCredentials("", false)))
	// if err != nil {
	// 	return nil, err
	// }

	// // Create the client
	// client := NewAnchorServiceClient(conn)
	// res, err := client.SubmitProof()
}
